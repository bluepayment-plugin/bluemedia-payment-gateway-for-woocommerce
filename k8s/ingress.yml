apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: web-wc
  namespace: bm-ecommerce-test
spec:
  rules:
  - host: bluepayment-wc.bm.bscloud.pl
    http:
      paths:
      - backend:
          serviceName: web-wc
          servicePort: http
